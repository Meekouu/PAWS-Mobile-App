import 'package:flutter/material.dart';

class SavedArticlesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Saved Articles')),
      body: Center(
        child: Text('This is where saved articles would be listed.'),
      ),
    );
  }
}
